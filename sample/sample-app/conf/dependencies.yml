# Application dependencies

require:
    - play
    - nl-> selenium 0.1
repositories:
    - MyMmodules:
        type:       local
        artifact:   ${application.path}/../../../[module]
        contains:
            - nl -> *

